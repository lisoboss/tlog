---
import { formatDate } from '../utils/date';

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-ctp-subtext0">
  {formatDate(date)}
</time>